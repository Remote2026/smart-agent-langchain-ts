OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_API_KEY=replace-me
OPENAI_MODEL=qwen-turbo

SMARTTHINGS_PAT=replace-me
ROSBRIDGE_URL=ws://localhost:9090

PORT=3000


SLACK_ENABLED=true
SLACK_BOT_TOKEN=replace-me
SLACK_APP_TOKEN=replace-me
SLACK_SIGNING_SECRET=replace-me
SLACK_MIRROR_WEB_MESSAGES=false
SLACK_DEFAULT_CHANNEL_ID=C0B2VCZ4C78

# DeepSeek V4 模型适配：在 thinking mode 下，API 要求将 reasoning_content 原样传回，
# 否则多轮对话（特别是 tool call 后）会返回 400 错误。
# 使用 DeepSeek V4 模型时设为 true，其他模型保持 false（默认）。
DEEPSEEK_REASONING_FIX=false